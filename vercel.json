{
    "installCommand": "cd eventsphere && npm install",
    "buildCommand": "cd eventsphere && npm run build",
    "outputDirectory": "eventsphere/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}